<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.summerProject.ink_book.Mapper.GroupMapper">
    <select id="selectGroupById" resultType="com.summerProject.ink_book.Entity.Group">
        select * from ink_book.`groups` where groupId = #{id}
    </select>
    <insert id="insertGroup" parameterType="com.summerProject.ink_book.Entity.Group" useGeneratedKeys="true" keyProperty="groupId">
        insert into ink_book.`groups`(ink_book.`groups`.groupName, ink_book.`groups`.groupCreateTime, ink_book.`groups`.groupProfile)
        VALUES (#{groupName}, #{groupCreateTime}, #{groupProfile})
    </insert>
    <select id="selectGroupByMem" parameterType="com.summerProject.ink_book.Entity.User" resultType="com.summerProject.ink_book.Entity.Group">
        select * from ink_book.`groups` a, ink_book.group_user b where a.groupId=b.groupId and b.userId=#{userId}
    </select>
    <select id="selectGroupByFounder" parameterType="com.summerProject.ink_book.Entity.User" resultType="com.summerProject.ink_book.Entity.Group">
        select * from ink_book.`groups` a, ink_book.group_user b where a.groupId=b.groupId and b.userId=#{userId} and b.userLevel=3
    </select>
</mapper>